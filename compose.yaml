services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    environment:
      DATABASE_URL: sqlite:////data/invoice_management.db
      SEED_DEMO_DATA: "true"
      JWT_SECRET_KEY: ${JWT_SECRET_KEY:?JWT_SECRET_KEY must be set in .env}
      JWT_ACCESS_TOKEN_EXPIRE_MINUTES: ${JWT_ACCESS_TOKEN_EXPIRE_MINUTES:-60}
      DEMO_ADMIN_USERNAME: ${DEMO_ADMIN_USERNAME:-admin}
      DEMO_ADMIN_PASSWORD: ${DEMO_ADMIN_PASSWORD:?DEMO_ADMIN_PASSWORD must be set in .env}
      DEMO_COMPANY_MANAGER_USERNAME: ${DEMO_COMPANY_MANAGER_USERNAME:-companymanager}
      DEMO_COMPANY_MANAGER_PASSWORD: ${DEMO_COMPANY_MANAGER_PASSWORD:?DEMO_COMPANY_MANAGER_PASSWORD must be set in .env}
      DEMO_COMPANY_VIEWER_USERNAME: ${DEMO_COMPANY_VIEWER_USERNAME:-companyviewer}
      DEMO_COMPANY_VIEWER_PASSWORD: ${DEMO_COMPANY_VIEWER_PASSWORD:?DEMO_COMPANY_VIEWER_PASSWORD must be set in .env}
      CORS_ALLOWED_ORIGINS: "${CORS_ALLOWED_ORIGINS:-}"
    volumes:
      - invoice-data:/data
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://localhost:8000/').read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 10s
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      args:
        NEXT_PUBLIC_API_BASE_URL: "${NEXT_PUBLIC_API_BASE_URL:-http://localhost:8000}"
    ports:
      - "3000:3000"
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

volumes:
  invoice-data:
    name: invoice-management-data